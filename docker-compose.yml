services:
  slate-docs-prod:
    build:
      context: .
    container_name: slate-docs-prod
    volumes:
      - .:/srv/slate
    ports:
      - "4567:4567"
    command: ["serve"]   # overrides CMD ["build"]
